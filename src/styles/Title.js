import { Reaction, Box, Font, Align, Gap } from 'themeor'

// You don't really need this component, it's only for styling
export function Title(props) {
  return (
    <Font size="20px" weight="700" {...props} />
  )
}
